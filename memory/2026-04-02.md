# 2026-04-02 - OpenClaw 完整設定記錄

## 🎯 今日工作總結

### 1. 模型配置調整
- **主力模型**：改為 `openrouter/free`（自動選擇免費模型池中的最佳模型）
- **備用模型**：`anthropic/claude-sonnet-4-6`（付費，僅在任務需要時使用）
- **Ollama 14B 整合**：
  - 已安裝 `qwen3:14b-opt` 模型
  - 配置 `baseUrl: http://localhost:11434`
  - 問題：OpenClaw 的 Ollama provider 初始化失敗，暫時無法使用
  - 狀態：保留配置，待後續 debug

### 2. 語音 (TTS) 功能完整設定
- **安裝**：`pip3 install edge-tts`
- **腳本**：
  - `/Users/ericsu/.openclaw/workspace/create-voice.py` - 生成 MP3
  - `/Users/ericsu/.openclaw/workspace/send-voice.sh` - 發送語音到 Telegram
  - `/Users/ericsu/.openclaw/workspace/send-voice.sh` 支援所有 `send-*.sh` 腳本
- **語音**：`zh-CN-XiaoxiaoNeural`（中文女聲）
- **API**：Telegram Bot `sendVoice`（原生語音泡泡，有波形圖）
- **白名單**：所有語音相關腳本和命令已加入「永遠允許」

### 3. 執行批准策略優化
- **安全策略**：`allowlist`（只允許白名單內的命令）
- **自動執行**：
  - 常用命令：`date`, `cat`, `echo`, `curl`, `head`, `tail`, `grep`, `ps`, `sleep`
  - 工具：`python3`, `npm`, `pip3`, `openclaw`, `ollama`, `which`
  - 語音腳本：`*.py`, `send-*.sh`, `chmod`
- **需要批准**：
  - 危險命令：`rm -rf`, 上傳金鑰/帳密，修改系統設定
  - Inline 代碼執行：`python3 -c`, `node -e`（`strictInlineEval: true`）
- **批准方式**：Telegram 聊天輸入 `/approve <id> allow-once|allow-always|deny`

### 4. Telegram 批准通知設定
- **啟用**：`execApprovals.enabled: true`
- **模式**：DM 模式（私人對話收到批准通知）
- **命令**：在 Telegram 直接用 `/approve` 批准，不需切換 Web UI

## 📝 關鍵配置檔案

### `~/.openclaw/openclaw.json`
```json
{
  "models": {
    "providers": {
      "ollama": {
        "baseUrl": "http://localhost:11434",
        "models": [{"id": "qwen3:14b-opt", "name": "qwen3:14b-opt"}]
      }
    }
  },
  "agents": {
    "defaults": {
      "model": {
        "primary": "openrouter/free",
        "fallbacks": ["anthropic/claude-sonnet-4-6", ...]
      },
      "sandbox": {"mode": "off"}
    }
  },
  "tools": {
    "exec": {"strictInlineEval": true}
  },
  "approvals": {
    "exec": {
      "enabled": true,
      "mode": "targets",
      "targets": [{"channel": "telegram", "to": "8397380746"}]
    }
  },
  "channels": {
    "telegram": {
      "execApprovals": {"enabled": true}
    }
  }
}
```

### `~/.openclaw/exec-approvals.json`
- **defaults.security**: `allowlist`
- **agents.main.security**: `allowlist`
- **白名單**：
  - 常用命令：`date`, `cat`, `echo`, `curl`, `python3`, `npm`, `pip3`, `chmod` 等
  - 語音腳本：`/Users/ericsu/.openclaw/workspace/*.py`, `send-*.sh`
  - 工具：`openclaw`, `ollama`, `which`, `head`, `tail`, `grep`, `ps`

## 🚀 使用方式

### 語音回覆
- 說「**用語音回答**」或「**用語音回答：[內容]**」
- 自動生成 MP3 + 發送 Telegram 語音泡泡
- **完全不需批准**

### 日常命令
- 讀取檔案、搜尋網頁、執行系統命令
- **完全不需批准**

### 危險命令
- 刪除檔案、上傳金鑰/帳密、修改系統設定
- 會在 Telegram 收到批准通知
- 輸入 `/approve <id> allow-always` 永久允許

## 🔧 待辦事項
- [ ]  debug Ollama 14B 整合問題（OpenClaw provider 初始化失敗）
- [ ] 測試更多語音功能（不同語音、速度、語氣）

## 📊 當前模型
- **主力**：`openrouter/free` → 自動選擇免費模型（目前為 Qwen 3.5 122B）
- **備用**：`anthropic/claude-sonnet-4-6`（付費）
- **Ollama**：`qwen3:14b-opt`（已配置，待 debug）

---
**設定完成時間**：2026-04-02 12:00
**設定者**：蝦老大
**AI 助手**：爆蝦 🦐
